// Deprecated registry removed by route-based migration.
// All tabs now have dedicated pages under `app/(app)/<tab>/page.jsx`.
// This stub remains temporarily to avoid import errors on stale branches.
export function getViewByKey() {
  return null
}
